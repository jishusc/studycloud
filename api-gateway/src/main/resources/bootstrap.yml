spring:
  application:
    name: api-gateway  cloud:    config:      discovery:        enabled: true
        service-id: config-server
      profile: dev
        
eureka:  client:    service-url:      defaultZone: http://vm1:8761/eureka

apollo:
  bootstrap:
    enabled: true
  meta: http://apollodev.stosystem.com:58080
app:
  id: YW04_005      