spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev    
    sentinel:
      log:
        dir: ./logs/
        switch-pid: true
      transport:
        dashboard: http://10.33.74.155:7001
      datasource:
        flow:
          apollo:
            namespace-name: application
            flow-rules-key: ${spring.application.name}-flow-rules
            default-flow-rule-value: 
            rule-type: flow  
eureka:  client:    service-url:      defaultZone: http://vm1:8761/eureka

apollo:
  meta: http://192.168.122.72:8080
app:
  id: YW01_product
